services:
  archiver:
    build: .
    ports:
      - "${ARCHIVER_PORT:-8080}:8080"
    environment:
      ARCHIVER_ANTHROPIC_API_KEY: ${ARCHIVER_ANTHROPIC_API_KEY}
    volumes:
      - ${ARCHIVER_DATA_PATH:-./data}:/data
    restart: unless-stopped
